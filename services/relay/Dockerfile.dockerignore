# Context for services/relay/Dockerfile (BuildKit uses this over a root
# .dockerignore). Excludes everything except the image's inputs.
*
!package.json
!bun.lock
!tsconfig.json
!core/**
!client/**
!services/relay/**
!test/vectors/relay-policy.test.ts
